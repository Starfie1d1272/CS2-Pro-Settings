name: Daily update

on:
  schedule:
    # 08:17 Asia/Shanghai (UTC+8) = 00:17 UTC
    - cron: "17 0 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run: live fetch + analysis, no issues/commits/PRs"
        type: boolean
        default: true

concurrency:
  # shared with weekly-reconcile: never let two production workflows write
  # the runtime state cache concurrently
  group: cs2-pro-settings-production
  cancel-in-progress: true

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          fetch-depth: 0

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.11"
          cache: pip

      - name: Install
        run: pip install -e ".[dev]"

      - name: Restore runtime state cache
        id: restore-state
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: .runtime-state
          key: cs2-pro-settings-runtime-main-${{ github.run_id }}
          restore-keys: cs2-pro-settings-runtime-main-

      - name: Run pipeline (live, scheduled sources only)
        id: pipeline
        continue-on-error: true
        run: python -m cs2_pro_settings update --scheduled

      - name: Decide and act (issues / candidate PR)
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DRY_RUN: ${{ github.event_name == 'schedule' && 'false' || github.event.inputs.dry_run }}
          PIPELINE_OUTCOME: ${{ steps.pipeline.outcome }}
        run: python scripts/actions_daily.py

      - name: Save runtime state cache (production success only)
        if: steps.pipeline.outcome == 'success' && github.event_name == 'schedule'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: .runtime-state
          key: cs2-pro-settings-runtime-main-${{ github.run_id }}

      - name: Final status gate
        if: steps.pipeline.outcome == 'failure'
        run: |
          echo "pipeline failed; source-health issue was raised; workflow stays red"
          exit 1
