# v2 runtime dependencies (canonical source: pyproject.toml).
# This file is a compatibility entrypoint for pip users; it mirrors the
# [project.dependencies] table exactly.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0

# dev
pytest>=7.0
