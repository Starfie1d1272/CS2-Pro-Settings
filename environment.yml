# v2 root environment (canonical source: pyproject.toml).
# Minimal: matches pyproject.toml [project.dependencies].
# Historical v1 notebook reproduction deps live in notebooks/v1/environment.yml.
name: cs2_data
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pandas>=2.0
  - numpy>=1.24
  - matplotlib>=3.7
  - requests>=2.31
  - beautifulsoup4>=4.12
  - pyyaml>=6.0
  - pytest>=7.0
  - pip
